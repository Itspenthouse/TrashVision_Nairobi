[pytest]
# `pythonpath = .` puts the ml/ folder on Python's import path when tests run,
# so `from app.scoring import ...` works. `testpaths` tells pytest where to look.
pythonpath = .
testpaths = tests
addopts = -v
